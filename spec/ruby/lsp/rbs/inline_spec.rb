# frozen_string_literal: true

RSpec.describe Ruby::Lsp::Rbs::Inline do
  it "has a version number" do
    expect(Ruby::Lsp::Rbs::Inline::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
